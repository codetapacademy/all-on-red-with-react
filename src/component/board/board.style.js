import styled from 'styled-components'

export const StyledBoard = styled.div`
  background-color: #94632c;
  border-radius: 50%;
  width: ${({ size }) => `${size}px`};
  height: ${({ size }) => `${size}px`};
`
